/**
 * Per-section editor for the storefront content, reached from /site.
 * Stub — the real editor lands with the next task; the route exists so the
 * Site cards already link somewhere.
 */
export default function SiteSectionEdit() {
  return null;
}
